import 'package:flutter/material.dart';

// SiteUpdateScreen — will be fully built in later phases
class SiteUpdateScreen extends StatelessWidget {
  const SiteUpdateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SiteUpdateScreen')),
      body: const Center(
        child: Text('Coming soon', style: TextStyle(color: Colors.white54)),
      ),
    );
  }
}
