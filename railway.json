{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && node --max-old-space-size=460 node_modules/.bin/nest build"
  },
  "deploy": {
    "startCommand": "cd backend && node --max-old-space-size=460 dist/main",
    "restartPolicyType": "ON_FAILURE"
  }
}
